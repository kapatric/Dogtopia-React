import React, { Component } from "react";
import App from "./App.js"

/*
const ImageSlider = ({ slides }) => { 

  // setCurrentIndex(slides[0])
  const slideString = '"' + slides[0] + '"'
console.log(slides)

  const sliderStyles = {
    height: "100%",
    position: "realtive",
  };
  const slideStyles = {
    width: "100%",
    height: "100%",
    borderRadius: "10px",
    backgroaundPosition: "center",
    backgroundSize: "cover",
    backgroundImage: "",
  };

  const leftArrowStyle = {
    position: "absolute",
    top: "50%",
    tranform: "translate(0, -50%)",
    left: "32px",
    fontsize: "45px",
    color: "#fff",
    cursor:"pointer"
  }

  const rightArrowStyle = {
    position: "absolute",
    top: "50%",
    tranform: "translate(0, -50%)",
    right: "32px",
    fontsize: "45px",
    color: "#fff",
    cursor:"pointer"
  }

  const moveLeft = () => {

  }

  const moveRight = () => {

  }

  return (
    <div style={sliderStyles} className="Slider">
      <div style={leftArrowStyle} onClick={moveLeft}>L</div>
      <div style={rightArrowStyle} onClick={moveRight}>R</div>
      <div style={slideStyles}>
      <img src={"https://cdn2.thedogapi.com/images/BJa4kxc4X.jpg"}/>
      </div>
    </div>
  );
};
export default ImageSlider;
*/
